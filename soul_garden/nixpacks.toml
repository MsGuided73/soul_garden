# Nixpacks configuration for Soul Garden Monorepo

[phases.setup]
nixPkgs = ["nodejs-22_x"]

[phases.install]
cmds = ["cd frontend && npm install"]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "cd frontend && npm run preview -- --host 0.0.0.0 --port 8000"
